/**
 * Development-only fallback — NOT used when the API is reachable.
 * Enable with NEXT_PUBLIC_ALLOW_DEMO_FALLBACK=true only after a failed inference.
 */
export const DEV_MOCK_FALLBACK_NOTE =
  "Demo fallback — API unavailable. Set NEXT_PUBLIC_ALLOW_DEMO_FALLBACK=true to enable.";
